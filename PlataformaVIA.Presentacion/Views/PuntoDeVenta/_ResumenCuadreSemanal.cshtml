@model IEnumerable<PlataformaVIA.Core.Domain.PuntoDeVenta.ResumenCuadreSemanal>

@*<div class="modal-dialog modal-lg">
    <div class="modal-content">
        <div class="modal-header">
            <button class="close" aria-hidden="true" type="button" data-dismiss="modal">×</button>
            <h4 class="modal-title" id="myModalLabel">Resumen Cuadre Semanal</h4>
        </div>
        <div class="modal-body">*@
@*<h4>RESUMEN CUADRE SEMANAL</h4>*@
<table class="table table-striped table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tipo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LineaDeNegocio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Valor)
        </th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LineaDeNegocio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td class="text-right">
                @Html.DisplayFor(modelItem => item.Valor)
            </td>
        </tr>
    }

    <tr>
        <td colspan="3">
            <span><b>Total Cuadre Semanal</b></span>
        </td>
        <td class="text-right">
            <span><b>@Model.Sum(mm => mm.Valor).ToString("C")</b></span>
        </td>
    </tr>

</table>
@*</div>
        </div>
    </div>*@
