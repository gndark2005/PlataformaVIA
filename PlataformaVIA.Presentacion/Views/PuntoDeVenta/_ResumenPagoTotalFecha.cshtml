@model IEnumerable<PlataformaVIA.Core.Domain.PuntoDeVenta.ResumenPago>

@*<div class="modal-dialog modal-lg">
    <div class="modal-content">
        <div class="modal-header">
            <button class="close" aria-hidden="true" type="button" data-dismiss="modal">×</button>
            <h4 class="modal-title" id="myModalLabel">RESUMEN PAGO TOTAL A LA FECHA</h4>
        </div>
        <div class="modal-body">*@
@*<h4 title title--sub w-100 text text--color--blue border border--bottom border--color--blue pb-1 bg bg--color--white text-left> RESUMEN PAGO TOTAL A LA FECHA</h4>*@
<table class="table table-striped table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Concepto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Valor)
        </th>
    </tr>

    @foreach (var item in Model)
    {

        if (Model.Last().Equals(item))
        {
            <tr>
                <td>
                    <span><b>@Html.DisplayFor(modelItem => item.Concepto)</b></span>
                </td>
                <td class="text-right">
                    <span><b>@Html.DisplayFor(modelItem => item.Valor)</b></span>
                </td>
            </tr>
        }
        else
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Concepto)
                </td>
                <td class="text-right">
                    @Html.DisplayFor(modelItem => item.Valor)
                </td>
            </tr>
        }
    }

</table>
@*</div>
        </div>
    </div>*@
